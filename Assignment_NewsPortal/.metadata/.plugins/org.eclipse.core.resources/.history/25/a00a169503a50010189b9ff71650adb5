@charset "UTF-8";

/* ====== Admin Users — Clean CSS ====== */
/* Màu chủ đạo xanh lá*/
:root{
  --bg: #f7f9fb;
  --card: #ffffff;
  --text: #1f2937;
  --muted: #6b7280;
  --primary: #16a34a;      /* xanh lá */
  --primary-600:#15803d;
  --accent: #10b981;       /* xanh ngọc dịu */
  --danger: #ef4444;
  --danger-600:#dc2626;
  --ring: rgba(16,185,129,.25);
  --border: #e5e7eb;
  --shadow: 0 8px 24px rgba(0,0,0,.06);
}

@media (prefers-color-scheme: dark){
  :root{
    --bg: #0b1220;
    --card: #0f172a;
    --text: #e5e7eb;
    --muted: #94a3b8;
    --primary: #22c55e;
    --primary-600:#16a34a;
    --accent: #34d399;
    --danger: #f87171;
    --danger-600:#ef4444;
    --ring: rgba(52,211,153,.25);
    --border: #1f2a44;
    --shadow: 0 10px 24px rgba(0,0,0,.35);
  }
}

/* Tổng thể */
html,body{
  background: var(--bg);
  color: var(--text);
  font-family: system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol";
  line-height: 1.5;
}

/* Tiêu đề */
h2{
  font-size: clamp(20px, 2.4vw, 28px);
  font-weight: 800;
  letter-spacing: -0.02em;
  margin: 16px 0 20px;
  display: inline-flex;
  align-items: center;
  gap: 10px;
}
h2::after{
  content:"";
  display:inline-block;
  width: 46px;
  height: 6px;
  border-radius: 999px;
  background: linear-gradient(90deg, var(--primary), var(--accent));
}

/* Hàng người dùng */
.user-row.card{
  display: grid;
  grid-template-columns: 1fr auto;
  align-items: center;
  gap: 16px;
  padding: 16px 18px;
  border: 1px solid var(--border);
  border-radius: 16px;
  background: var(--card);
  box-shadow: var(--shadow);
  transition: transform .18s ease, box-shadow .18s ease, border-color .18s ease;
  will-change: transform;
  margin-bottom: 12px;
}
.user-row.card:hover{
  transform: translateY(-2px);
  box-shadow: 0 14px 36px rgba(0,0,0,.10);
  border-color: rgba(16,185,129,.35);
}

/* Khối thông tin người dùng (bên trái) */
.user-row > div{
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  gap: 8px 12px;
  min-width: 0;
}
.user-row strong{
  font-weight: 700;
}
.user-row strong + span,
.user-row strong + .badge,
.user-row strong + .badge + .badge{
  margin-left: 6px;
}
.user-row .muted{ color: var(--muted); }

.badge{
  display: inline-flex;
  align-items: center;
  gap: 6px;
  padding: 4px 10px;
  border-radius: 999px;
  font-size: 12px;
  font-weight: 700;
  letter-spacing: .2px;
  background: rgba(16,185,129,.08);
  color: var(--primary);
  border: 1px solid rgba(16,185,129,.20);
}
.badge.active{
  background: rgba(34,197,94,.10);
  color: #16a34a;
  border-color: rgba(34,197,94,.22);
}
.badge.inactive{
  background: rgba(148,163,184,.12);
  color: #64748b;
  border-color: rgba(148,163,184,.28);
}

.actions{
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  gap: 8px;
  justify-content: flex-end;
  margin: 0;
}

/* Button base */
.btn{
  --btn-bg: var(--primary);
  --btn-bg-h: var(--primary-600);
  --btn-fg: #fff;
  --btn-bd: transparent;

  appearance: none;
  border: 1px solid var(--btn-bd);
  background: var(--btn-bg);
  color: var(--btn-fg);
  padding: 10px 14px;
  border-radius: 12px;
  font-weight: 700;
  font-size: 14px;
  letter-spacing: .2px;
  cursor: pointer;
  transition: transform .12s ease, filter .12s ease, background .12s ease, border-color .12s ease;
  outline: none;
}

/* Ghost (nền trong suốt, viền) */
.btn.ghost{
  --btn-bg: transparent;
  --btn-fg: var(--text);
  --btn-bd: var(--border);
  background: linear-gradient(var(--card), var(--card)) padding-box,
              linear-gradient(135deg, rgba(16,185,129,.45), rgba(59,130,246,.35)) border-box;
  border: 1px solid transparent;
}
.btn.ghost:hover{
  filter: brightness(1.02);
}

/* Danger */
.btn.danger{
  --btn-bg: var(--danger);
  --btn-bg-h: var(--danger-600);
}
.btn:hover{
  transform: translateY(-1px);
  filter: brightness(1.03);
  background: var(--btn-bg-h);
}
.btn:active{
  transform: translateY(0);
  filter: none;
}

.btn[disabled], .btn:disabled{
  opacity: .55;
  cursor: not-allowed;
  transform: none;
  filter: none;
}


.btn:focus-visible, .badge:focus-visible, .user-row.card:focus-within{
  box-shadow: 0 0 0 3px var(--ring);
}


.actions input[type="hidden"]{ display:none; }


.user-row.card:last-of-type{ margin-bottom: 24px; }

/* Responsive */
@media (max-width: 900px){
  .user-row.card{
    grid-template-columns: 1fr;
    gap: 12px;
  }
  .actions{
    justify-content: flex-start;
  }
}
@media (max-width: 520px){
  .btn{ padding: 9px 12px; font-size: 13px; }
  .badge{ font-size: 11px; padding: 3px 8px; }
  .user-row > div{ gap: 6px 10px; }
}


main, .container, .content{
  
  max-width: 1100px;
  margin-inline: auto;
  padding-inline: 16px;
}
