/* =========================
   NewsPortal – Base Styles
   Author: you ✨
   ========================= */

/* -------- Color system (light) -------- */
:root{
  --bg: #0b0c10;                 /* page background (dark gradient base) */
  --surface: #101318;            /* main card/nav background */
  --surface-2: #151a21;          /* subtle alt surface */
  --text: #e7ecf3;               /* primary text */
  --muted: #9fb0c2;              /* secondary text */
  --brand: #5aa9ff;              /* brand accent */
  --brand-2: #7c5cff;            /* accent 2 */
  --danger: #ff6b6b;
  --success:#33d69f;
  --warning:#ffbf69;

  --radius: 16px;
  --shadow: 0 10px 30px rgba(0,0,0,.35);
  --border: 1px solid rgba(255,255,255,.06);
}

/* Optional: auto dark via system preference (kept same palette) */
@media (prefers-color-scheme: light) {
  :root{
    --bg: #f6f7fb;
    --surface: #ffffff;
    --surface-2: #f0f2f7;
    --text: #1c212b;
    --muted: #5b667a;
    --brand: #2563eb;
    --brand-2: #7c3aed;
    --shadow: 0 12px 22px rgba(15,23,42,.1);
    --border: 1px solid rgba(0,0,0,.06);
  }
}

/* -------- Resets -------- */
*{ box-sizing: border-box; }
html,body{ height:100%; }
html{ scroll-behavior:smooth; }
body{
  margin:0;
  font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, "Helvetica Neue", Arial, "Noto Sans", "Liberation Sans", sans-serif;
  color:var(--text);
  background:
    radial-gradient(1200px 600px at 100% -10%, rgba(124,92,255,.25), transparent 40%),
    radial-gradient(1200px 600px at -10% 120%, rgba(90,169,255,.25), transparent 40%),
    var(--bg);
  line-height:1.6;
}

/* Utilities */
.container{
  width:min(1100px, 92%);
  margin-inline:auto;
  padding: 24px 0 64px;
}
.mt-24{ margin-top:24px;}
.hidden{ display:none !important; }

a{ color:var(--brand); text-decoration:none; }
a:hover{ text-decoration:underline; }
img{ max-width:100%; display:block; border-radius: calc(var(--radius) - 6px); }

/* -------- Header / Nav -------- */
header{
  background: linear-gradient(180deg, rgba(255,255,255,.04), transparent),
              var(--surface);
  border-bottom: var(--border);
  position: sticky; top:0; z-index:50;
  backdrop-filter: blur(6px);
}
.nav{
  display:flex; align-items:center; gap:16px;
  justify-content:space-between;
  padding: 14px 0;
}
.brand{
  display:flex; align-items:center; gap:10px; font-weight:800; letter-spacing:.3px;
  color:var(--text);
}
.brand .dot{
  width:12px; height:12px; border-radius:999px; background:linear-gradient(135deg,var(--brand),var(--brand-2));
  box-shadow: 0 0 0 4px rgba(124,92,255,.12);
}
.menu{ display:flex; gap:10px; flex-wrap:wrap; }
.menu a{
  color:var(--muted);
  padding:8px 12px;
  border-radius:10px;
  transition:.2s ease;
}
.menu a:hover{
  color:var(--text);
  background:var(--surface-2);
}

/* -------- Layout: main grid -------- */
.grid{
  display:grid;
  grid-template-columns: 1fr 320px;
  gap:28px;
}
.content{
  background:var(--surface);
  border:var(--border);
  border-radius:var(--radius);
  box-shadow:var(--shadow);
  padding:22px;
}
.sidebar{
  position: sticky; top:90px;
  align-self:start;
  background:var(--surface);
  border:var(--border);
  border-radius:var(--radius);
  box-shadow:var(--shadow);
  padding:18px;
}
.sidebar h3{
  margin:8px 0 10px; font-size:15px; color:var(--muted); font-weight:700; text-transform:uppercase; letter-spacing:.08em;
}
.sidebar ul{ list-style:none; padding:0; margin:0; display:grid; gap:6px; }
.sidebar a{
  display:block; padding:9px 10px; border-radius:10px; color:var(--text);
}
.sidebar a:hover{ background:var(--surface-2); }

/* -------- Sections / titles -------- */
h1,h2,h3{ margin: 6px 0 14px; line-height:1.25; }
h1{ font-size: clamp(22px, 2.6vw, 32px); }
h2{ font-size: clamp(18px, 2vw, 24px); color:var(--muted); }

/* -------- Cards / Lists -------- */
.list-posts{
  display:grid;
  grid-template-columns: repeat(3, 1fr);
  gap:18px;
}
.card{
  background:var(--surface-2);
  border:var(--border);
  border-radius:var(--radius);
  overflow:hidden;
  transition: transform .15s ease, box-shadow .15s ease, border-color .15s ease;
}
.card:hover{
  transform: translateY(-3px);
  box-shadow: 0 14px 30px rgba(0,0,0,.18);
  border-color: rgba(124,92,255,.35);
}
.card .body{ padding:14px 14px 16px; }
.card h2, .card h3{
  margin:6px 0 8px; font-size:18px; color:var(--text);
}
.card .meta{
  font-size:12px; color:var(--muted);
}
.list{ list-style:none; padding:0; margin:0; display:grid; gap:10px; }
.list li{
  background:var(--surface-2);
  border:var(--border);
  border-radius:12px;
  padding:10px 12px;
}
.list li a{ color:var(--text); }
.badge{
  display:inline-block; padding:2px 8px; border-radius:999px;
  font-size:11px; letter-spacing:.02em; background:rgba(124,92,255,.15); color:var(--brand-2);
}

/* -------- Forms -------- */
.form{ display:grid; gap:12px; }
.form label{ font-weight:600; color:var(--muted); font-size:13px; }
.form input[type="text"],
.form input[type="email"],
.form input[type="password"],
.form input[type="date"],
.form input[type="file"],
.form select,
.form textarea{
  width:100%; padding:10px 12px;
  border-radius:12px; border:var(--border);
  background:var(--surface-2);
  color:var(--text);
  outline:none; transition: border-color .2s, box-shadow .2s;
}
.form textarea{ min-height:140px; resize:vertical; }
.form input:focus,
.form select:focus,
.form textarea:focus{
  border-color: rgba(90,169,255,.55);
  box-shadow: 0 0 0 4px rgba(90,169,255,.15);
}
.inline{ display:inline-flex; gap:6px; align-items:center; }
.actions{ display:flex; gap:10px; flex-wrap:wrap; margin-top:6px; }

.btn{
  --b: var(--brand);
  display:inline-flex; align-items:center; gap:8px;
  padding:10px 14px; border-radius:12px; border:1px solid color-mix(in oklab, var(--b), #000 10%);
  font-weight:700; color:#fff; background: linear-gradient(180deg, color-mix(in oklab, var(--b), #fff 6%), var(--b));
  box-shadow: 0 8px 18px color-mix(in oklab, var(--b), #000 80% / 20%);
  cursor:pointer; transition: transform .12s ease, box-shadow .12s ease, filter .12s ease;
}
.btn:hover{ transform: translateY(-1px); filter:brightness(1.02); }
.btn:active{ transform: translateY(0); }

.btn.secondary{ --b: var(--brand-2); }
.btn.ghost{
  --b: var(--surface-2);
  background:var(--surface-2);
  border:var(--border);
  color:var(--text);
  box-shadow:none;
}

/* -------- Tables (admin) -------- */
.table{
  width:100%; border-collapse:collapse; font-size:14px; overflow:hidden;
  border-radius:14px; box-shadow:var(--shadow); background:var(--surface);
}
.table th, .table td{
  padding:12px 14px; border-bottom: var(--border);
  text-align:left;
}
.table thead th{
  background: linear-gradient(180deg, rgba(124,92,255,.10), transparent);
  color:var(--muted); text-transform:uppercase; font-size:12px; letter-spacing:.06em;
}
.table tr:hover td{ background:rgba(124,92,255,.06); }
.table .actions a{
  margin-right:8px; padding:6px 10px; border-radius:10px; background:var(--surface-2); border:var(--border);
  color:var(--text); text-decoration:none; display:inline-block;
}
.table .actions a:hover{ background:rgba(124,92,255,.15); }

/* -------- Alerts / chips -------- */
.alert{
  padding:12px 14px; border-radius:12px; border:var(--border);
  background:rgba(90,169,255,.12); color:var(--text);
}
.alert.error{ background: rgba(255,107,107,.16); border-color: rgba(255,107,107,.25); }
.alert.success{ background: rgba(51,214,159,.16); border-color: rgba(51,214,159,.25); }
.chip{
  display:inline-flex; align-items:center; gap:8px; padding:6px 10px; border-radius:999px;
  background:var(--surface-2); border:var(--border); font-size:12px; color:var(--muted);
}

/* -------- Footer -------- */
footer{
  margin-top:40px; padding:26px 0; color:var(--muted); text-align:center;
}

/* -------- Admin shell -------- */
.admin-header{
  background: var(--surface);
  border-bottom: var(--border);
  box-shadow: var(--shadow);
}
.admin-nav{
  display:flex; gap:12px; flex-wrap:wrap; padding:12px 0;
}
.admin-nav a{
  color:var(--muted); padding:8px 12px; border-radius:10px;
}
.admin-nav a:hover{ color:var(--text); background:var(--surface-2); }
.admin-container{ padding-top:20px; }
.admin-footer{ color:var(--muted); text-align:right; padding:12px 0; }

/* -------- Search bar (optional) -------- */
.searchbar{
  display:flex; gap:10px; align-items:center; margin:8px 0 16px;
}
.searchbar input[type="text"]{
  flex:1; padding:12px 14px; border-radius:12px; border:var(--border); background:var(--surface-2); color:var(--text);
}
.searchbar .btn{ white-space:nowrap; }

/* -------- Responsive -------- */
@media (max-width: 1024px){
  .grid{ grid-template-columns: 1fr; }
  .sidebar{ position:static; order:-1; }
  .list-posts{ grid-template-columns: repeat(2, 1fr); }
}
@media (max-width: 640px){
  .menu{ gap:6px; }
  .list-posts{ grid-template-columns: 1fr; }
  .nav{ flex-wrap:wrap; gap:8px; }
  .container{ width:94%; }
}

/* -------- Tiny helpers for spacing -------- */
hr{ border:0; border-top: var(--border); margin:20px 0; opacity:.7; }
.meta{ color:var(--muted); font-size:12px; }
.center{ text-align:center; }
